name: Upload

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    uses: tau-OS/github-actions/.github/workflows/build-flatpak.yml@main
    with:
      manifest: "./co.tauos.Sdk.json"
    secrets:
      repoToken: ${{ secrets.FLATPAK_REPO_TOKEN }}
